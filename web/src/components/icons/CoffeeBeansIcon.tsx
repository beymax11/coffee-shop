import type { LucideProps } from "lucide-react";
import { Bean } from "lucide-react";

/** Brand logo mark — coffee beans (Lucide Bean). */
export function CoffeeBeansIcon(props: LucideProps) {
  return <Bean {...props} />;
}
